apiVersion : v1
kind: Pod
metadata:
 name: my-web
 labels: 
  env: prod
  app: main
  tier: frontend
  owner: VikVo
spec:
 containers:
  - name: container-apache
    image: vikvo11/k8sphp:latest
    ports:
     - containerPort: 80
